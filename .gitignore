*.pyc
data/raw/
__pycache__/
*.pyc
# Ignore the large model file
models/third_party/arcface.onnx
# virtualenv
.venv/

# editor backups from the instrumentation pass
*.bak

# --- added by workspace cleanup 2026-09-04 ---
# Python environments & caches
venv/
env/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# OS / editor cruft
Thumbs.db
Desktop.ini
.DS_Store
~$*
*.tmp

# LaTeX build artifacts
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.log
*.out
*.synctex.gz
*.toc
*.nav
*.snm

# --- added by repo cleanup 2026-09-20 ---
# Large binaries: keep them on disk, out of git
*.rar
*.zip

backup-DO-NOT-touch.rar

# LaTeX previews and superseded drafts
paper/pg-*.png
paper/build.*

# Stray root artefacts


# --- added by repo reorganization 2026-09-23 ---
# Session videos show faces; the run directories around them are committed.
outputs/runs/**/*.mp4
outputs/runs/**/*.avi

# Personal material that is not the project: backups, old submissions,
# poster, paper history, scratch. Kept on disk, never committed.
_local/
# Built manuscripts; regenerate from paper/*.tex
paper/*.pdf
